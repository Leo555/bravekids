# 复制为 .env.local（本地调试用），线上请配到 Vercel 项目的 Environment Variables。
# 真实值绝对不要提交到仓库。
#
# 注意：本地开发这两项都可以不填！
#   - 没填 Redis → 存档自动写到 .dev-store.json
#   - 没填口令   → 本地默认口令是 dev
# 只有想连线上真实 Redis 调试时才需要填。

# ---------------------------------------------------------------------------
# 1) Redis：必须是 REST 协议的 Upstash Redis（redis:// TCP 连接串不支持）
#    Vercel 项目 → Storage → Upstash for Redis → 连接到项目后会自动注入，
#    线上不用手填；本地可以用 `npx vercel env pull .env.local` 拉下来。
#    下面两组命名代码都兼容，填哪组都行。
# ---------------------------------------------------------------------------
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# ---------------------------------------------------------------------------
# 2) 家庭口令：访问云端存档的唯一凭证，请设一个够长的随机串（建议 16 位以上）
#    生成：openssl rand -base64 18
#    线上没设置这一项时，接口会返回 503，App 打不开。
#    本地开发没设置时，默认口令是 dev。
# ---------------------------------------------------------------------------
FAMILY_PASSCODE=
